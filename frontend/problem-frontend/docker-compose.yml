version: '3.8'

services:
  problem-frontend:
    build: .
    ports:
      - "9002:9002"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=development
